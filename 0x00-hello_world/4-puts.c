#include <stdio.h>
/**
 * main - A program that prints lines with the put function
 * Return : 0 (when the program returns 0, then we are successful)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
