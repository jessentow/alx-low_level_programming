#include "main.h"
#include <unistd.h>
/**
 * _putchar - notes the char c to stdout
 * @c: The char to print
 * Return: success 1.
 * error: -1 is returned, and error is set appropriately.
 */
int _putchar(char c)
{
	return (write(1, &c, 1));
}
