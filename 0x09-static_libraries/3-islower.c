#include "main.h"
/**
 * _islower - This function  checks for lowercase character
 * @c: This is the  char  to check
 * Return: This will return 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
